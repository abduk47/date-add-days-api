{
  "name": "date-add-days-api",
  "version": "1.3.0",
  "description": "HTTP API: add integer days to a date (ISO or {seconds,nanos}), and parse comma-separated strings into arrays. Returns YYYY-MM-DD, YYYY-MM-DDTHH:MM:SSZ, and {seconds,nanos}.",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "swagger-ui-express": "^5.0.1"
  }
}